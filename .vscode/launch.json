{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome with Live Server",
            // This is the default URL for Live Server.
            // You might need to change the '5500' port.
            "url": "http://127.0.0.1:5500/index.html",
            "webRoot": "${workspaceFolder}",
        }
    ]
}